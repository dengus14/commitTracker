{
  "name": "commit-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm start --prefix did-you-code-today\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
